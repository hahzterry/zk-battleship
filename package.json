{
  "name": "shadowfleet",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve:dist": "node scripts/serve-dist.mjs",
    "serve:dist:nocoi": "node scripts/serve-dist.mjs --no-coi",
    "serve:dist:subpath": "node scripts/serve-dist.mjs --mount /shadowfleet/",
    "clean": "node scripts/clean.mjs",
    "package:itch": "npm run build && node scripts/package-itch.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@provablehq/sdk": "^0.11.6",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "vite": "^6.4.3"
  }
}
